var name = 'max';
var age =29;
var hasHobbies = true;

function summarize(userName, userAge, userHasHooby) {
    retrun (
        'Name is ' +
        userName +
        ', age is ' +
        userAge +
        ' and the user has hobbies' +
        userHasHooby
    );
}

console.log(summarize(name,age,hasHobbies))